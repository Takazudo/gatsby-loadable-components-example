import * as React from "react";

const StaticComponent = () => {
  return (
    <div>
      This is static component! <br />I don't need JavaScript!
    </div>
  );
};

export default StaticComponent;
